# 场景上传管理器 - 演示指南

## 🎬 快速演示

### 第一步：启动服务器

```bash
cd web-uploader
./start.sh  # macOS/Linux
```

你会看到：

```
==================================
🚀 场景上传管理器
==================================

✅ Python 版本: Python 3.x.x

📦 检查依赖...
✅ 依赖已安装

🚀 启动 API 服务器...

============================================================
🚀 场景上传管理器 API 服务器
============================================================
📁 项目根目录: /Users/.../x-pilot-video-render
📂 场景目录: /Users/.../x-pilot-video-render/src/scenes
📄 Manifest: /Users/.../x-pilot-video-render/src/scenes/manifest.json
============================================================
🌐 API 端点:
  GET  /health          - 健康检查
  GET  /scenes          - 获取场景列表
  POST /push            - 推送单个场景
  POST /push-batch      - 批量推送场景
  DELETE /delete/<id>   - 删除场景
  POST /set-theme       - 设置主题
  GET  /get-theme       - 获取主题
  GET  /manifest        - 获取 manifest
============================================================
🔗 访问地址: http://localhost:8000
🌐 前端界面: http://localhost:8000
============================================================

 * Running on http://0.0.0.0:8000
```

### 第二步：打开浏览器

访问: **http://localhost:8000**

你会看到一个漂亮的渐变背景界面！

---

## 📸 界面演示

### 1️⃣ 上传单个场景

**操作步骤：**

1. 点击 **"📤 上传场景"** 标签（默认已选中）

2. 点击右上角的 **"📝 加载示例"** 按钮，表单会自动填充：
   ```
   场景 ID: scene-example
   场景名称: DNA 介绍
   持续帧数: 90
   文件名: (空)
   主题: 科技蓝
   场景代码: (完整的 React 组件代码)
   ```

3. 点击底部的 **"🚀 上传场景"** 按钮

4. 看到绿色成功提示：
   ```
   ✅ 场景上传成功！
   ```

**结果：**
- 文件 `src/scenes/scene-example.tsx` 已创建
- `manifest.json` 已更新
- 主题设置为 `tech`

---

### 2️⃣ 批量上传场景

**操作步骤：**

1. 点击 **"📦 批量上传"** 标签

2. 点击右上角的 **"📝 加载示例"** 按钮，文本框会填充 JSON 数组：
   ```json
   [
     {
       "id": "scene-1",
       "name": "标题介绍",
       "duration": 90,
       "content": "import React from \"react\";\n..."
     },
     {
       "id": "scene-2",
       "name": "内容讲解",
       "duration": 150,
       "content": "import React from \"react\";\n..."
     }
   ]
   ```

3. 点击底部的 **"🚀 批量上传"** 按钮

4. 看到成功提示：
   ```
   ✅ 批量上传成功！2/2 个场景
   ```

**结果：**
- 创建了 2 个场景文件
- `manifest.json` 批量更新

---

### 3️⃣ 查看场景列表

**操作步骤：**

1. 点击 **"📋 场景列表"** 标签

2. 看到所有已上传的场景，每个场景显示：
   ```
   ┌─────────────────────────────────────────┐
   │ 1  DNA 介绍  [scene-example]            │
   │    📄 scene-example.tsx                 │
   │    ⏱️ 90 帧 (3.0s)                      │
   │                        [🗑️ 删除]        │
   └─────────────────────────────────────────┘
   
   ┌─────────────────────────────────────────┐
   │ 2  标题介绍  [scene-1]                  │
   │    📄 scene-1.tsx                       │
   │    ⏱️ 90 帧 (3.0s)                      │
   │                        [🗑️ 删除]        │
   └─────────────────────────────────────────┘
   
   ┌─────────────────────────────────────────┐
   │ 3  内容讲解  [scene-2]                  │
   │    📄 scene-2.tsx                       │
   │    ⏱️ 150 帧 (5.0s)                     │
   │                        [🗑️ 删除]        │
   └─────────────────────────────────────────┘
   ```

3. 顶部显示总时长：
   ```
   总时长: 11.0s (330 帧)
   ```

**删除场景：**
- 点击任意场景的 **"🗑️ 删除"** 按钮
- 确认删除
- 场景文件和 manifest 条目被删除
- 列表自动刷新

---

### 4️⃣ 管理配置

**操作步骤：**

1. 点击 **"⚙️ 配置"** 标签

2. 查看视频配置信息：
   ```
   ┌──────────────┐  ┌──────────────┐
   │ 分辨率       │  │ 帧率         │
   │ 1920 × 1080  │  │ 30 fps       │
   └──────────────┘  └──────────────┘
   
   ┌──────────────┐  ┌──────────────┐
   │ 当前主题     │  │ 版本         │
   │ tech         │  │ 1.0.0        │
   └──────────────┘  └──────────────┘
   ```

3. 切换主题 - 点击任意主题按钮：
   ```
   [科技蓝] [赛博朋克] [优雅紫]
   [自然绿] [温暖橙]   [极简黑白]
   ```

4. 看到成功提示：
   ```
   ✅ 主题已设置为: cyberpunk
   ```

5. 查看 `manifest.json` 预览：
   ```json
   {
     "version": "1.0.0",
     "fps": 30,
     "width": 1920,
     "height": 1080,
     "theme": "cyberpunk",
     "scenes": [...]
   }
   ```

---

## 🎯 实际使用示例

### 示例 1: 上传一个数学教学场景

**场景代码：**

```tsx
import React from "react";
import { AbsoluteFill } from "remotion";
import { MathFunctionPlot } from "../components/science-math";
import { TitleBasic } from "../components/narrative-typography";

export default function MathScene() {
  return (
    <AbsoluteFill style={{ backgroundColor: "#0f172a", padding: 40 }}>
      <TitleBasic text="二次函数图像" />
      <MathFunctionPlot 
        functions={[
          { expression: "x^2", color: "#3b82f6" },
          { expression: "x^2 + 2*x + 1", color: "#8b5cf6" }
        ]}
        xRange={[-5, 5]}
        yRange={[-2, 10]}
      />
    </AbsoluteFill>
  );
}
```

**上传步骤：**
1. 场景 ID: `math-quadratic`
2. 场景名称: `二次函数图像`
3. 持续帧数: `180` (6秒)
4. 主题: `tech`
5. 粘贴上面的代码
6. 点击上传

---

### 示例 2: 批量上传一个完整的教学视频

**JSON 数据：**

```json
[
  {
    "id": "intro",
    "name": "课程介绍",
    "duration": 90,
    "content": "import React from \"react\";\nimport { AbsoluteFill } from \"remotion\";\nimport { TitleCinematicIntro } from \"../components/narrative-typography\";\n\nexport default function Intro() {\n  return (\n    <AbsoluteFill>\n      <TitleCinematicIntro title=\"Python 编程入门\" subtitle=\"从零开始学编程\" />\n    </AbsoluteFill>\n  );\n}"
  },
  {
    "id": "chapter1",
    "name": "第一章：变量与数据类型",
    "duration": 150,
    "content": "import React from \"react\";\nimport { AbsoluteFill } from \"remotion\";\nimport { ListStaggeredEntry } from \"../components/narrative-typography\";\n\nexport default function Chapter1() {\n  return (\n    <AbsoluteFill style={{ backgroundColor: \"#0f172a\", padding: 40 }}>\n      <ListStaggeredEntry items={[\n        \"整数 (int)\",\n        \"浮点数 (float)\",\n        \"字符串 (str)\",\n        \"布尔值 (bool)\"\n      ]} />\n    </AbsoluteFill>\n  );\n}"
  },
  {
    "id": "code-demo",
    "name": "代码演示",
    "duration": 180,
    "content": "import React from \"react\";\nimport { AbsoluteFill } from \"remotion\";\nimport { TechTerminalTyping } from \"../components/tech-code-demo\";\n\nexport default function CodeDemo() {\n  return (\n    <AbsoluteFill>\n      <TechTerminalTyping commands={[\n        \"python3\",\n        \">>> x = 10\",\n        \">>> print(x)\",\n        \"10\"\n      ]} />\n    </AbsoluteFill>\n  );\n}"
  },
  {
    "id": "conclusion",
    "name": "课程总结",
    "duration": 90,
    "content": "import React from \"react\";\nimport { AbsoluteFill } from \"remotion\";\nimport { QuoteParallaxBg } from \"../components/narrative-typography\";\n\nexport default function Conclusion() {\n  return (\n    <AbsoluteFill>\n      <QuoteParallaxBg text=\"编程改变世界\" author=\"Steve Jobs\" />\n    </AbsoluteFill>\n  );\n}"
  }
]
```

**上传步骤：**
1. 切换到 "📦 批量上传" 标签
2. 粘贴上面的 JSON
3. 点击 "🚀 批量上传"
4. 看到成功提示：`批量上传成功！4/4 个场景`

**结果：**
- 创建了 4 个场景文件
- 总时长：17 秒 (510 帧)
- 完整的教学视频结构

---

## 🧪 测试 API

运行测试脚本验证所有功能：

```bash
cd web-uploader
python test_api.py
```

**测试输出：**

```
============================================================
🧪 API 测试脚本
============================================================

🔍 测试健康检查...
   状态码: 200
   响应: {'status': 'ok', 'message': 'Scene Uploader API is running'}

🔍 测试获取场景列表...
   状态码: 200
   场景数量: 3

🔍 测试推送单个场景...
   状态码: 200
   响应: {'success': True, 'message': '场景 测试场景 推送成功'}

🔍 测试批量推送...
   状态码: 200
   响应: {'success': True, 'message': '批量推送完成', 'success_count': 2, 'total': 2}

🔍 测试设置主题...
   状态码: 200
   响应: {'success': True, 'message': '主题已设置为: cyberpunk'}

🔍 测试获取主题...
   状态码: 200
   响应: {'success': True, 'theme': 'cyberpunk'}

🔍 测试获取场景列表...
   状态码: 200
   场景数量: 6

🔍 测试删除场景...
   状态码: 200
   响应: {'success': True, 'message': '场景 test-scene-1 已删除'}

============================================================
✅ 所有测试完成！
============================================================
```

---

## 💡 使用技巧

### 技巧 1: 快速填充表单

点击 **"📝 加载示例"** 按钮可以快速填充示例数据，然后修改需要的字段。

### 技巧 2: 帧数计算

输入帧数后，下方会自动显示对应的秒数：
```
持续帧数: 90
3.0 秒 (30fps)  ← 自动计算
```

### 技巧 3: JSON 格式化

在批量上传前，可以使用在线 JSON 格式化工具验证格式：
- https://jsonformatter.org/
- https://jsonlint.com/

### 技巧 4: 保存 JSON 备份

将批量上传的 JSON 保存为文件，方便下次使用：
```bash
# 保存
cat > scenes.json << 'EOF'
[
  {"id": "scene-1", ...},
  {"id": "scene-2", ...}
]
EOF

# 使用
# 复制 scenes.json 内容到批量上传页面
```

### 技巧 5: 查看实时日志

服务器终端会显示所有请求日志：
```
127.0.0.1 - - [08/Dec/2024 17:58:23] "GET /scenes HTTP/1.1" 200 -
127.0.0.1 - - [08/Dec/2024 17:58:25] "POST /push HTTP/1.1" 200 -
✅ 场景文件已写入: /Users/.../src/scenes/scene-1.tsx
➕ 新增场景: DNA 介绍
✅ Manifest 已更新
```

---

## 🎨 界面特色

### 渐变背景
- 深色主题，护眼舒适
- 蓝紫渐变，科技感十足

### 毛玻璃效果
- 卡片背景半透明
- 模糊效果，层次分明

### 响应式设计
- 自适应不同屏幕尺寸
- 移动端友好

### 动画效果
- 按钮悬停效果
- 消息淡入淡出
- 平滑过渡

---

## 🚀 开始你的演示

1. **启动服务器**
   ```bash
   cd web-uploader
   ./start.sh
   ```

2. **打开浏览器**
   ```
   http://localhost:8000
   ```

3. **开始上传场景**
   - 单个上传：适合测试和学习
   - 批量上传：适合生产环境

4. **管理你的场景**
   - 查看列表
   - 删除场景
   - 切换主题

**享受使用场景上传管理器！** 🎉

---

## 📞 需要帮助？

- 查看 [README.md](README.md) 了解详细文档
- 查看 [WEB_UPLOADER_GUIDE.md](../WEB_UPLOADER_GUIDE.md) 快速指南
- 运行 `python test_api.py` 测试 API
- 查看服务器终端日志排查问题
